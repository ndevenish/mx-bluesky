env:
  sources:
    - kind: deviceManager
      module: dodal.beamlines.i24
    # The web UI plans, and the serial plans the edms still invoke by name. Remove
    # the second source once the edms are gone.
    - kind: planFunctions
      module: mx_bluesky.beamlines.i24
    - kind: planFunctions
      module: mx_bluesky.beamlines.i24.serial
  events:
    broadcast_status_events: false
api:
  url: http://localhost:25565
  cors:
    allow_credentials: True
    origins: 
      - "*"
stomp:
  enabled: true
  url: tcp://i24-control.diamond.ac.uk:61613
logging:
  level: "DEBUG"
  graylog:
    url: "tcp://graylog-log-target.diamond.ac.uk:12231"
    enabled: true
